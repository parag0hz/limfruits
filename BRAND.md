# 임과일 (limfruits) 브랜드 가이드

나주배 농가 직판 사이트. 로고의 손그림 감성(귀엽고 따뜻한 농부 캐릭터)을 웹 전체에 일관되게 적용한다.

## 컬러 (로고에서 실측 추출)

| 토큰 | 값 | 용도 |
|---|---|---|
| `--color-brand` | `#417837` | 메인 그린. 헤더/푸터 배경, 버튼, 강조 텍스트, 테두리 |
| `--color-brand-dark` | `#2F5928` | 그린 hover/진한 텍스트 |
| `--color-brand-light` | `#EAF2E6` | 연한 그린 배경(뱃지, 섹션 배경) |
| `--color-cream` | `#FBF8EF` | 페이지 기본 배경 (순백 대신 따뜻한 크림) |
| `--color-accent-red` | `#DE432D` | 포인트 (품절/취소 뱃지, 강조) |
| `--color-accent-yellow` | `#F1D14A` | 포인트 (하이라이트, 별점 등) |
| `--color-accent-pink` | `#E8789C` | 포인트 (아주 절제해서 사용) |
| `--color-ink` | `#2B3327` | 본문 텍스트 (녹색기 도는 진한 잉크색) |

Tailwind v4 `@theme`으로 등록해 `bg-brand`, `text-ink` 등으로 사용.

## 타이포그래피

- 제목: **Jua** (`next/font/google`) — 로고의 둥글둥글한 손글씨 느낌과 어울림
- 본문: **Noto Sans KR** 400/500/700
- 가격 숫자는 700 이상으로 크고 명확하게

## 스타일 원칙 (손그림 감성)

- 카드/버튼: `rounded-2xl`~`rounded-3xl`, 진한 그린 `2px` 실선 테두리 → 손으로 그린 스티커 느낌
- 로고의 흰색 아치(무지개 모양) 모티프를 히어로 섹션 등에 곡선 형태로 재사용
- 배경은 크림색, 섹션 구분은 연그린/화이트 교차
- 그림자는 부드럽고 최소한으로, 대신 테두리로 구조를 잡음
- 사진이 아직 없으므로 로고 일러스트(`/logo.jpeg`)와 CSS 도형/이모지 없이 절제된 일러스트 느낌으로 구성. 가짜 사진 넣지 말 것 (실사진 자리는 명확한 placeholder 처리)

## 로고 자산

- `public/logo.jpeg` — 정방형 (초록 배경 포함, 667×667). 히어로/OG 이미지용
- `public/logo-long.png` — 가로형 "임과일" 워드마크 (초록 배경). 헤더용
- `app/icon.jpg` — 파비콘 (Next.js 자동 인식)

헤더가 그린 배경(`#417837`)이면 `logo-long.png`가 자연스럽게 어울림 (로고 배경색과 동일).

## 카피 톤

- 따뜻한 존댓말. "부모님이(가족이) 직접 농사지은 나주배를 산지에서 바로 보내드립니다" 방향
- 확인되지 않은 사실(수상 이력, N대째 전통 등) 지어내지 말 것 — 구체 정보가 필요한 자리는 PLACEHOLDERS.md에 기록하고 눈에 띄는 placeholder로 표시
- 브랜드명 표기: 한글 "임과일", 영문 소문자 "limfruits"
